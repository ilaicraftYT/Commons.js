let {
    getRandomFromArray,
    cloneArray
} = require("./helpers/arrays")
let {
    parseNum
} = require("./helpers/string")

module.exports = {
    getRandomFromArray,
    cloneArray,
    parseNum
}